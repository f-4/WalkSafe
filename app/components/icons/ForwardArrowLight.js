import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const forwardArrowLight = (<Icon name="arrow-forward" size={30} color="#fff" />);

export default forwardArrowLight;

import React, { Component } from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

export const menuIcon = (<Icon name="bars" size={30} color="#000" />);
export const locationIcon = (<Icon name="location-arrow" size={30} color="#000" />);
